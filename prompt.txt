create a react app (JS not typescript ) that lets me build chord progressions using a node system.  where users construct chord progressions through dynamic suggestions rather than manual trial-and-error, a directed graph node system is the ideal data structure. In this model, every chord is a Node, and music theory rules (voice leading, functional harmony, modal interchange) dictate the Edges (valid pathways) between them.

Instead of a rigid flowchart, treating the chord space as a probabilistic network allows the app to offer two distinct psychological modes when a user selects a starting chord: Build (tension-increasing movement, extensions, modal color) and Resolve (tension-releasing movement, cadence fulfillment).

hmmm i was thinking that the app should a ui with all chords that exist and letting me select the initial chord for the first node. i should be able to add new node and lead the previous node to it. that new node i added i can lead back to 1st node for a "loop" if i point it back at the 1st node, the app will suggest to me only chords that "resolve" back to 1st node.  if i was to add a new node without loop-back arrow, then it will suggest to me chords that "build" the progression.